# Importing the necessary libraries
import geopandas as gpd
import matplotlib.pyplot as plt

# Step 1: Load a shapefile (replace with your shapefile path)
# For demonstration, we'll use a built-in dataset from GeoPandas
world = gpd.read_file(gpd.datasets.get_path('naturalearth_lowres'))

# Step 2: Check the data (you can inspect the first few rows)
print(world.head())

# Step 3: Plotting the world map
world.plot()

# Step 4: Adding customizations (optional)
plt.title("World Map - Natural Earth Low Resolution")
plt.show()
